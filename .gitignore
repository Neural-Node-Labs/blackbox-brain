# Secrets — never commit real environment files
.env
.env.*
!.env.example

# Node
agent/node_modules/
agent/dist/
agent/*.tsbuildinfo
npm-debug.log*

# Python
gateway/**/__pycache__/
gateway/**/*.pyc
.venv/
venv/

# OS / editor
.DS_Store
*.swp

# SSH config — targets.json has no secrets but known_hosts is host-specific
ssh/known_hosts
/.idea/
/ollama_models/
